package graff.opmatematicas.funciones;

import graff.coordenadas.Posiciones;
import graff.opmatematicas.CalculadoraFuncional;

public class Coseno {
/*    private double amplitud;
    private double frecuencia;
    private double fase;

    public Coseno() {
    }

    public Coseno(double amplitud, double frecuencia, double fase) {
        this.amplitud = amplitud;
        this.frecuencia = frecuencia;
        this.fase = fase;
    }

    public double crearOperacion(double x, Posiciones posiciones) {
        return amplitud * Math.cos(2 * Math.PI * frecuencia * x + fase);

    }


    public Posiciones calcularValores(int desde, int hasta) {
        Posiciones posiciones = new Posiciones(); // crear una instancia de Posiciones
        for (int x = desde; x <= hasta; x++) {
            double y = crearOperacion(x, posiciones);
            posiciones.agregarCoordenadas(x, y); // agregar coordenadas al objeto Posiciones
        }
        return posiciones;
    }

 */
}
